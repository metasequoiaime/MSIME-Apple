name: Native Platform CI

# Native-host gates for the four platforms that do not use the Apple runners.
# Release workflows remain separate; this workflow only builds and tests.
on:
  push:
    branches: [develop, main]
    paths-ignore:
      - CHANGELOG.md
      - PRIVACY.md
      - README.en.md
      - README.md
      - docs/**
  pull_request:
    paths-ignore:
      - CHANGELOG.md
      - PRIVACY.md
      - README.en.md
      - README.md
      - docs/**
  workflow_dispatch:
  merge_group:

concurrency:
  group: platform-ci-${{ github.workflow }}-${{ github.event_name }}-${{ github.event.pull_request.number || github.ref_name }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  changes:
    name: Decide platform scope
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    outputs:
      android: ${{ steps.decide.outputs.android }}
      linux: ${{ steps.decide.outputs.linux }}
      harmonyos: ${{ steps.decide.outputs.harmonyos }}
      windows: ${{ steps.decide.outputs.windows }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - id: decide
        env:
          BASE_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -euo pipefail
          if [[ "$GITHUB_EVENT_NAME" != push && "$GITHUB_EVENT_NAME" != pull_request ]] ||
             [[ ! "$BASE_SHA" =~ ^[0-9a-f]{40}$ ]] || ! git cat-file -e "$BASE_SHA" 2>/dev/null; then
            printf 'android=true\nlinux=true\nharmonyos=true\nwindows=true\n' >> "$GITHUB_OUTPUT"
            exit 0
          fi
          changed=$(git diff --name-only "$BASE_SHA" "$HEAD_SHA")
          shared=false
          android=false; linux=false; harmonyos=false; windows=false
          while IFS= read -r path; do
            [[ -z "$path" ]] && continue
            case "$path" in
              platforms/android/*) android=true ;;
              platforms/linux/*) linux=true ;;
              platforms/harmony/*) harmonyos=true ;;
              platforms/windows/*) windows=true ;;
              shared/*|vendor/*|crates/*|packages/*|apps/*|engine-lock.json|product-lock.json|scripts/*|cmake/*|CMakeLists.txt|.github/workflows/ci-platforms.yml)
                shared=true ;;
            esac
          done <<< "$changed"
          if [[ "$shared" == true ]]; then
            android=true; linux=true; harmonyos=true; windows=true
          fi
          printf 'android=%s\nlinux=%s\nharmonyos=%s\nwindows=%s\n' \
            "$android" "$linux" "$harmonyos" "$windows" >> "$GITHUB_OUTPUT"

  android:
    needs: changes
    name: Android host
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Install ripgrep
        if: ${{ needs.changes.outputs.android == 'true' }}
        run: |
          sudo apt-get update
          sudo apt-get install --yes ripgrep
      - name: Check Android host contracts and JVM smoke tests
        if: ${{ needs.changes.outputs.android == 'true' }}
        env:
          ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
        run: bash platforms/android/check-host.sh
      - name: Android platform unchanged
        if: ${{ needs.changes.outputs.android != 'true' }}
        run: echo 'No Android or shared-platform changes; platform gate skipped.'

  linux:
    needs: changes
    name: Linux native host
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Build IBus and Fcitx5 hosts in the pinned container
        if: ${{ needs.changes.outputs.linux == 'true' }}
        run: bash platforms/linux/build-container.sh
      - name: Linux platform unchanged
        if: ${{ needs.changes.outputs.linux != 'true' }}
        run: echo 'No Linux or shared-platform changes; platform gate skipped.'

  harmonyos:
    needs: changes
    name: HarmonyOS host
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      # The settings bundle below is a committed build artefact, so the toolchain that produces it has to be the same one contributors run; the runner's default Node is not pinned to anything.
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        if: ${{ needs.changes.outputs.harmonyos == 'true' }}
        with:
          node-version-file: .nvmrc
      - name: Install Node dependencies
        if: ${{ needs.changes.outputs.harmonyos == 'true' }}
        run: |
          corepack enable
          pnpm install --frozen-lockfile --ignore-scripts
      - name: Type-check and run Harmony keyboard logic tests
        if: ${{ needs.changes.outputs.harmonyos == 'true' }}
        run: bash platforms/harmony/tests/run.sh
      # The settings window is a WebView over a generated artefact committed to the repository, and nothing rebuilds it. It sat fifty-two commits out of date once, and it goes stale again on every merge that touches packages/ui -- where the only thing that notices is whoever next runs verify-local.sh, and until they regenerate it the pre-push hook rejects everyone else's pushes too. This job already runs for every shared-UI change and already has pnpm, which is all the check needs.
      - name: Check the settings bundle matches the shared UI
        if: ${{ needs.changes.outputs.harmonyos == 'true' }}
        run: python3 scripts/test-harmony-settings-bundle.py
      - name: HarmonyOS platform unchanged
        if: ${{ needs.changes.outputs.harmonyos != 'true' }}
        run: echo 'No HarmonyOS or shared-platform changes; platform gate skipped.'

  windows:
    needs: changes
    name: Windows GNU cross build
    runs-on: ubuntu-24.04
    # Ubuntu 24.04's MinGW headers predate d2d1_3.h, which msimeui includes for ID2D1DeviceContext5 and SVG rendering, so the build runs in the Debian release platforms/windows/cross/Dockerfile is based on. The steps below mirror that Dockerfile.
    container: debian:trixie-slim
    timeout-minutes: 90
    steps:
      - name: Install MinGW cross toolchain
        if: ${{ needs.changes.outputs.windows == 'true' }}
        run: |
          apt-get update -qq
          apt-get install -y -qq --no-install-recommends g++-mingw-w64-x86-64 build-essential cmake ninja-build pkg-config curl ca-certificates git zip unzip tar python3
          # The unsuffixed compiler defaults to the win32 thread model, whose C++ library has no std::thread.
          update-alternatives --set x86_64-w64-mingw32-gcc /usr/bin/x86_64-w64-mingw32-gcc-posix
          update-alternatives --set x86_64-w64-mingw32-g++ /usr/bin/x86_64-w64-mingw32-g++-posix
          # Windows and macOS resolve `#include <Windows.h>` against the lowercase windows.h; this filesystem is case-sensitive, so supply the initial-capital spellings the sources use.
          for dir in /usr/share/mingw-w64/include /usr/x86_64-w64-mingw32/include; do
            [ -d "$dir" ] || continue
            for header in "$dir"/[a-z]*.h; do
              [ -e "$header" ] || continue
              name=$(basename "$header")
              capital="$(printf '%s' "$name" | cut -c1 | tr '[:lower:]' '[:upper:]')$(printf '%s' "$name" | cut -c2-)"
              [ -e "$dir/$capital" ] || ln -s "$name" "$dir/$capital"
            done
          done
          curl -sSf https://sh.rustup.rs | sh -s -- -y --profile minimal --default-toolchain none
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      # Checked out after git is installed, so the tree is a real repository rather than an API download.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Build Windows host, TSF DLL and native tests
        if: ${{ needs.changes.outputs.windows == 'true' }}
        run: |
          # The workspace is owned by the runner user, not the container's root.
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          # Installs the toolchain rust-toolchain.toml pins.
          rustup toolchain install
          bash platforms/windows/build-cross.sh x64
      - name: Windows platform unchanged
        if: ${{ needs.changes.outputs.windows != 'true' }}
        run: echo 'No Windows or shared-platform changes; platform gate skipped.'
