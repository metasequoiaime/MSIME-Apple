[package]
name = "msime-ios-native-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true

# The only unsafe code here is the small, reviewed boundary to Swift's @_cdecl
# exports. It is a separate crate so the Tauri command crate never has to carry
# that exemption; the exemption itself is a documented allow at the crate root.

[lints]
workspace = true
