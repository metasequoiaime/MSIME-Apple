[package]
name = "msime-ios-native-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true

# The only unsafe code in this crate is the small, reviewed boundary to Swift
# @_cdecl exports. Keep it out of the Tauri command crate, which inherits the
# workspace-wide unsafe_code = "forbid" policy.

[lints]
[lints.rust]
unsafe_code = "allow"
