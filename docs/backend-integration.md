# 全平台公共后端接入清单

范围：将已上线的公共 API 接到 Windows、macOS、iOS、Linux 的实际产品流程，补齐登录渠道配置、会话管理和显式同步。后端接口存在、客户端源码接入、合并发布、真实宿主验收分别记录，不互相替代。

## 必须完成

- [ ] Apple、Google、微信、手机、邮箱登录：渠道发现、挑战、凭据交换、取消、错误提示、资料、身份绑定、刷新、退出、注销账号。
- [ ] 各平台凭据存入系统安全存储；刷新并发协调、失效与退出清理；禁止共享设备令牌访问用户数据。
- [ ] 设置读取、版本冲突、明确选择上传/下载并应用可同步字段。
- [ ] 四类个人词库：目录、查询、增删改、文本导入导出、注音导入、增量变更；通过 Engine 应用到实际候选。
- [ ] 候选调频、固定位置、删除，跨端状态恢复、冲突与原子性；大快照使用有界流式文件传输。
- [ ] 剪贴板明确开启、显式添加、查询、去重、删除、清空和关闭；不默认上传系统剪贴板。
- [ ] 皮肤目录、资源、社区发布/下载/评分，校验后实际应用。
- [ ] 云候选、AI、翻译、批量和实时语音复用共通后端，保持取消、组合代次和失焦检查。
- [ ] 各输入查询、符号目录在相应产品入口可用，保持本地输入和离线回退。
- [ ] 各平台构建、协议集成测试、产品流程验收、PR 合并及发布；生产 Swagger/OpenAPI 保持关闭。

## 当前状态（2026-09-08）

- 后端 0.5.2 已上线；原有公共 API 抽取验收见 Backend 仓库 `docs/windows-api-extraction.md`。
- 线上 auth/providers：Apple 开启，Google、微信、手机、邮箱未开启。Apple 开启不代表真实账号登录验收通过。
- 已检查 Vault `msime-backend` 及 `common`、`notification`、`user` 的键名，没有发现可直接使用的新增登录渠道配置。不得挪用其他产品的 OAuth 客户端冒充本产品配置。
- 短信实现当前为阿里云短信，邮件实现为 SMTP；用户曾指明“短信用 Lark”，具体网关信息待确认。该配置问题不阻止其他客户端工作。
- iOS 账号基础与验证码/剪贴板接入已合入 develop（PR #349、#350）；Keychain、刷新协调、昵称修改、退出与注销已有实现。原生 Apple 真实签名账号及未开启渠道仍待验收。
- Windows、Apple、Linux 原工作区均有未提交适配与其他修改，保持原样。后续需逐项审查并将所需适配移到新基线，不能直接覆盖或把未提交代码当作已发布结果。

- iOS 已增加邮箱/手机号验证码登录页面：获取验证码、60 秒重发间隔、服务端有效期、取消和登录；入口只在渠道启用时出现。生产渠道仍关闭，尚无真实验证码验收。
- iOS 云剪贴板页面已接通明确开启/关闭、手动上传、搜索、复制、单条删除和清空。Swift 实际客户端连接生产 API 的同意、去重、特殊字符搜索、删除/关闭验收通过，临时账号与数据已清理；没有自动读取系统剪贴板。

- iOS 设置同步 PR #352 已合并：显式上传/下载，保留其他平台字段，使用版本冲突检查；13 项 Swift 测试、45 项项目检查和 Simulator 构建通过。实际客户端对生产完成设置读写、字段保留和过期版本拒绝；照片容量修复 Backend PR #12 已合并并随 0.5.2 部署，512,000 字节照片大小样例完整往返验证通过；K8s Synced/Healthy，Swagger/OpenAPI 均保持 404。
- iOS 云词库新增四类个人词条查询、分页、添加、编辑、版本约束删除及单条显式下载。实际 Swift 客户端对生产验证四类增删改查及过期版本删除拒绝，临时数据已清理。两项 Simulator 测试验证类型映射、Engine 校验及下载词条经队列进入键盘候选。批量跨端同步和大快照仍未完成；云端调频/固定位置页面进度见下。

- iOS 云词库文件页面已接入标准 TSV、Windows TSV 和汉字自动注音导入，选择文件先本机预览再确认上传。重复词条按服务端契约拒绝整批导入；导出支持受限流式文件传输和系统分享。生产验证四类格式往返、注音和 4,001 条超过 1 MiB 的标准/Windows 完整导出通过；临时账号、数据库记录和文件已清理。15 项 Swift 测试、45 项项目检查与完整 Simulator 构建通过；真实设备文件选择/分享操作尚待验收。

- iOS 已增加完整词库目录页面，支持全拼、四种双拼及其他类型编码查询、分页、基础/个人词条编辑与删除，按读取的全局词库版本拒绝冲突。实际 Swift 客户端连接生产验证四类基础目录修改、删除、过期版本拒绝和双拼查询通过；测试账号已清理。16 项 Swift 测试、45 项项目检查与完整 Simulator 构建通过。目录更改尚未通过完整快照同步应用到各平台本机。

- iOS 云端候选管理已接入候选查询、五种调频模式、触发次数/线性步长/强制置顶参数、固定位置查询/设置/取消，以及候选删除。修改使用服务端规范编码、上下文和读取时版本，保持 Engine 的计算职责；本机输入记录不会自动上传。生产验证五种调频模式、过期版本拒绝和双拼固定/取消/删除通过。18 项 Swift 测试、45 项项目检查、完整 Simulator 构建通过。完整快照应用到各端本机与真实宿主验收仍未完成。

- 完整同步共享前置接口已提交 Engine PR #79：流式导出日志状态，独占新代际准备，保留删除/新增标记、调频计数和固定位置；19 项 CTest 含十万条容量测试通过。尚未合并，下游未提前更新 Engine 固定版本。
- iOS 增加完整云快照备份导出，支持 NDJSON 流式下载、结束标记和 SHA-256 摘要检查，并保存校验摘要用于后续应用时复核。生产文件的版本、词条/覆盖/固定位置/选择计数记录验证通过，临时账号及文件已清理；合并设置同步基线后，24 项 Swift 测试与完整 Simulator 构建通过。这里仅验证传输完整性，严格记录语义校验、快照上传恢复和本机代际激活仍待接入。
