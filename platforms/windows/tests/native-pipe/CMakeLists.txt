add_library(msime-windows-pipe STATIC ../../PipeIo.cpp ../../PipePeer.cpp ../../PipeHandshake.cpp ../../PipeListener.cpp ../../PipeRegistry.cpp ../../PipeIntake.cpp ../../PipeService.cpp ../../PipeMainTransport.cpp ../../AuxListener.cpp)
target_include_directories(msime-windows-pipe PUBLIC ../..)
target_link_libraries(msime-windows-pipe PUBLIC advapi32 msime-windows-replies)
add_executable(windows-pipe-io ../pipe_io.cpp)
target_include_directories(windows-pipe-io PRIVATE ../../../../vendor/MSIME-Engine/contracts)
target_link_libraries(windows-pipe-io PRIVATE msime-windows-pipe advapi32)
foreach(target msime-windows-pipe windows-pipe-io)
  if(MSVC)
    target_compile_options(${target} PRIVATE /W4 /WX /utf-8)
  else()
    target_compile_options(${target} PRIVATE -Wall -Wextra -Werror)
  endif()
endforeach()
add_test(NAME windows-pipe-io COMMAND windows-pipe-io)
set_tests_properties(windows-pipe-io PROPERTIES TIMEOUT 30)

add_executable(windows-aux-listener ../aux_listener.cpp)
target_link_libraries(windows-aux-listener PRIVATE msime-windows-pipe)
if(MSVC)
  target_compile_options(windows-aux-listener PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-aux-listener PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-aux-listener COMMAND windows-aux-listener)
set_tests_properties(windows-aux-listener PROPERTIES TIMEOUT 30)
