cmake_minimum_required(VERSION 3.25)
include_directories("${CMAKE_CURRENT_SOURCE_DIR}/src/server" "${CMAKE_CURRENT_SOURCE_DIR}/src/ipc" "${CMAKE_CURRENT_SOURCE_DIR}/src/candidate" "${CMAKE_CURRENT_SOURCE_DIR}/src/voice" "${CMAKE_CURRENT_SOURCE_DIR}/src/clipboard" "${CMAKE_CURRENT_SOURCE_DIR}/src/input" "${CMAKE_CURRENT_SOURCE_DIR}/src/system")
project(MSIMEClientWindowsBoundary LANGUAGES CXX)
# State the Windows this targets instead of taking whatever the toolchain
# happens to default to. The code already requires 1703: per-monitor v2 DPI
# awareness and ID2D1DeviceContext5 both arrive there, and the latter sits
# behind NTDDI_WIN10_RS2 in the Direct2D headers. Toolchains disagree on the
# default - the build worked on one MinGW and failed to find that interface on
# another - which is the kind of difference a build should not be deciding by
# accident.
add_compile_definitions(_WIN32_WINNT=0x0A00 NTDDI_VERSION=0x0A000003)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
include(CTest)
enable_testing()
find_package(Threads REQUIRED)
add_executable(windows-translation-display tests/core/translation_display.cpp)
if(MSVC)
  target_compile_options(windows-translation-display PRIVATE /W4 /WX /utf-8 /UNDEBUG)
else()
  target_compile_options(windows-translation-display PRIVATE -Wall -Wextra -Werror -UNDEBUG)
endif()
add_test(NAME windows-translation-display COMMAND windows-translation-display)
add_executable(windows-paired-punctuation-host-policy tests/input/paired_punctuation_host_policy.cpp)
if(MSVC)
  target_compile_options(windows-paired-punctuation-host-policy PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-paired-punctuation-host-policy PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-paired-punctuation-host-policy COMMAND windows-paired-punctuation-host-policy)
set_tests_properties(windows-paired-punctuation-host-policy PROPERTIES TIMEOUT 10)
add_executable(windows-candidate-render-sync tests/ui/candidate_render_sync.cpp)
if(MSVC)
  target_compile_options(windows-candidate-render-sync PRIVATE /W4 /WX /UNDEBUG)
else()
  target_compile_options(windows-candidate-render-sync PRIVATE -Wall -Wextra -Werror -UNDEBUG)
endif()
add_test(NAME windows-candidate-render-sync COMMAND windows-candidate-render-sync)
set_tests_properties(windows-candidate-render-sync PROPERTIES TIMEOUT 10)
add_executable(windows-voice-theme tests/voice/voice_theme.cpp)
if(MSVC)
  target_compile_options(windows-voice-theme PRIVATE /W4 /WX /UNDEBUG)
else()
  target_compile_options(windows-voice-theme PRIVATE -Wall -Wextra -Werror -UNDEBUG)
endif()
add_test(NAME windows-voice-theme COMMAND windows-voice-theme)
set_tests_properties(windows-voice-theme PROPERTIES TIMEOUT 10)
add_executable(windows-wave-overlay-scale tests/voice/wave_overlay_scale.cpp)
if(MSVC)
  target_compile_options(windows-wave-overlay-scale PRIVATE /W4 /WX /UNDEBUG)
else()
  target_compile_options(windows-wave-overlay-scale PRIVATE -Wall -Wextra -Werror -UNDEBUG)
endif()
add_test(NAME windows-wave-overlay-scale COMMAND windows-wave-overlay-scale)
set_tests_properties(windows-wave-overlay-scale PROPERTIES TIMEOUT 10)
add_executable(windows-voice-controller-dispatch tests/voice/voice_controller_dispatch.cpp)
target_include_directories(windows-voice-controller-dispatch PRIVATE src ../../vendor/MSIME-Engine/contracts)
target_link_libraries(windows-voice-controller-dispatch PRIVATE Threads::Threads)
if(MSVC)
  target_compile_options(windows-voice-controller-dispatch PRIVATE /W4 /WX /utf-8 /UNDEBUG)
else()
  target_compile_options(windows-voice-controller-dispatch PRIVATE -Wall -Wextra -Werror -UNDEBUG)
endif()
add_test(NAME windows-voice-controller-dispatch COMMAND windows-voice-controller-dispatch)
set_tests_properties(windows-voice-controller-dispatch PROPERTIES TIMEOUT 10)
if(WIN32)
  add_executable(windows-candidate-font-format tests/ui/candidate_font_format.cpp)
  target_link_libraries(windows-candidate-font-format PRIVATE dwrite)
  if(MSVC)
    target_compile_options(windows-candidate-font-format PRIVATE /W4 /WX /UNDEBUG)
  else()
    target_compile_options(windows-candidate-font-format PRIVATE -Wall -Wextra -Werror -UNDEBUG)
  endif()
  add_test(NAME windows-candidate-font-format COMMAND windows-candidate-font-format)
  set_tests_properties(windows-candidate-font-format PROPERTIES TIMEOUT 10)
  add_executable(windows-voice-controller-listener tests/voice/voice_controller_listener.cpp src/ipc/PipeIo.cpp src/ipc/PipeListener.cpp src/ipc/PipePeer.cpp)
  target_include_directories(windows-voice-controller-listener PRIVATE src ../../vendor/MSIME-Engine/contracts)
  target_link_libraries(windows-voice-controller-listener PRIVATE advapi32 Threads::Threads)
  if(MSVC)
    target_compile_options(windows-voice-controller-listener PRIVATE /W4 /WX /utf-8 /UNDEBUG)
  else()
    target_compile_options(windows-voice-controller-listener PRIVATE -Wall -Wextra -Werror -UNDEBUG)
  endif()
  add_test(NAME windows-voice-controller-listener COMMAND windows-voice-controller-listener)
  set_tests_properties(windows-voice-controller-listener PROPERTIES TIMEOUT 15)
endif()
add_executable(windows-voice-review-result tests/voice/voice_review_result.cpp)
target_include_directories(windows-voice-review-result PRIVATE src ../../vendor/MSIME-Engine/contracts)
target_link_libraries(windows-voice-review-result PRIVATE Threads::Threads)
if(MSVC)
  target_compile_options(windows-voice-review-result PRIVATE /W4 /WX /utf-8 /UNDEBUG)
else()
  target_compile_options(windows-voice-review-result PRIVATE -Wall -Wextra -Werror -UNDEBUG)
endif()
add_test(NAME windows-voice-review-result COMMAND windows-voice-review-result)
set_tests_properties(windows-voice-review-result PROPERTIES TIMEOUT 10)
add_executable(windows-voice-controller-protocol tests/voice/voice_controller_protocol.cpp)
target_include_directories(windows-voice-controller-protocol PRIVATE src ../../vendor/MSIME-Engine/contracts)
if(MSVC)
  target_compile_options(windows-voice-controller-protocol PRIVATE /W4 /WX /utf-8 /UNDEBUG)
else()
  target_compile_options(windows-voice-controller-protocol PRIVATE -Wall -Wextra -Werror -UNDEBUG)
endif()
add_test(NAME windows-voice-controller-protocol COMMAND windows-voice-controller-protocol)
if(WIN32)
  add_executable(windows-voice-controller-connection tests/voice/voice_controller_connection.cpp src/ipc/PipeIo.cpp src/ipc/PipeListener.cpp src/ipc/PipePeer.cpp)
  target_include_directories(windows-voice-controller-connection PRIVATE src ../../vendor/MSIME-Engine/contracts)
  target_link_libraries(windows-voice-controller-connection PRIVATE advapi32 Threads::Threads)
  if(MSVC)
    target_compile_options(windows-voice-controller-connection PRIVATE /W4 /WX /utf-8 /UNDEBUG)
  else()
    target_compile_options(windows-voice-controller-connection PRIVATE -Wall -Wextra -Werror -UNDEBUG)
  endif()
  add_test(NAME windows-voice-controller-connection COMMAND windows-voice-controller-connection)
  set_tests_properties(windows-voice-controller-connection PROPERTIES TIMEOUT 10)
endif()
add_executable(windows-registration-inbox tests/runtime/registration_inbox.cpp)
target_include_directories(windows-registration-inbox PRIVATE src)
target_link_libraries(windows-registration-inbox PRIVATE Threads::Threads)
if(MSVC)
  target_compile_options(windows-registration-inbox PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-registration-inbox PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-registration-inbox COMMAND windows-registration-inbox)
set_tests_properties(windows-registration-inbox PROPERTIES TIMEOUT 10)
add_executable(windows-focus-router tests/runtime/focus_router.cpp)
target_include_directories(windows-focus-router PRIVATE src ../../vendor/MSIME-Engine/contracts)
target_link_libraries(windows-focus-router PRIVATE Threads::Threads)
if(MSVC)
  target_compile_options(windows-focus-router PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-focus-router PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-focus-router COMMAND windows-focus-router)
set_tests_properties(windows-focus-router PROPERTIES TIMEOUT 10)
add_executable(windows-main-frame tests/core/main_frame.cpp)
target_include_directories(windows-main-frame PRIVATE src ../../vendor/MSIME-Engine/contracts)
if(MSVC)
  target_compile_options(windows-main-frame PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-main-frame PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-main-frame COMMAND windows-main-frame)
add_executable(windows-focus-gate tests/runtime/focus_gate.cpp)
target_include_directories(windows-focus-gate PRIVATE src)
target_link_libraries(windows-focus-gate PRIVATE Threads::Threads)
if(MSVC)
  target_compile_options(windows-focus-gate PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-focus-gate PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-focus-gate COMMAND windows-focus-gate)
set_tests_properties(windows-focus-gate PROPERTIES TIMEOUT 10)
add_executable(windows-tsf-focus-lease-protocol tests/runtime/tsf_focus_lease_protocol.cpp)
target_include_directories(windows-tsf-focus-lease-protocol PRIVATE src)
if(MSVC)
  target_compile_options(windows-tsf-focus-lease-protocol PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-tsf-focus-lease-protocol PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-tsf-focus-lease-protocol COMMAND windows-tsf-focus-lease-protocol)
add_executable(windows-navigation-policy tests/input/navigation_policy.cpp)
find_package(nlohmann_json 3.11 REQUIRED)
target_include_directories(windows-navigation-policy PRIVATE src ../../vendor/MSIME-Engine/contracts ../../crates/host-api/include)
target_link_libraries(windows-navigation-policy PRIVATE nlohmann_json::nlohmann_json)
if(MSVC)
  target_compile_options(windows-navigation-policy PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-navigation-policy PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-navigation-policy COMMAND windows-navigation-policy)
add_executable(windows-typing-statistics tests/input/typing_statistics.cpp)
target_include_directories(windows-typing-statistics PRIVATE src/input)
target_link_libraries(windows-typing-statistics PRIVATE nlohmann_json::nlohmann_json)
if(MSVC)
  target_compile_options(windows-typing-statistics PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-typing-statistics PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-typing-statistics COMMAND windows-typing-statistics)
set_tests_properties(windows-typing-statistics PROPERTIES TIMEOUT 10)
# Which punctuation ends the composition with the highlighted candidate. The
# set is the reference's, character for character; one missing mark is a key
# that quietly stops doing that, visible only while typing.
# Which keys edit the composition at all: letters, the manual separator, the
# Japanese long vowel, the double-pinyin ing key, Unicode digits, erase and
# caret. Dense enough that a wrong branch is an input bug and nothing else.
add_executable(windows-edit-policy tests/input/edit_policy.cpp)
target_include_directories(windows-edit-policy PRIVATE src src/system src/input
                           ../../vendor/MSIME-Engine/contracts ../../crates/host-api/include)
target_link_libraries(windows-edit-policy PRIVATE nlohmann_json::nlohmann_json)
if(MSVC)
  target_compile_options(windows-edit-policy PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-edit-policy PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-edit-policy COMMAND windows-edit-policy)
set_tests_properties(windows-edit-policy PROPERTIES TIMEOUT 10)
add_executable(windows-punctuation-policy tests/input/punctuation_policy.cpp)
target_include_directories(windows-punctuation-policy PRIVATE src src/system src/input
                           ../../vendor/MSIME-Engine/contracts ../../crates/host-api/include)
target_link_libraries(windows-punctuation-policy PRIVATE nlohmann_json::nlohmann_json)
if(MSVC)
  target_compile_options(windows-punctuation-policy PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-punctuation-policy PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-punctuation-policy COMMAND windows-punctuation-policy)
set_tests_properties(windows-punctuation-policy PROPERTIES TIMEOUT 10)
add_executable(windows-input-key-policy tests/input/input_key_policy.cpp)
target_include_directories(windows-input-key-policy PRIVATE src)
if(MSVC)
  target_compile_options(windows-input-key-policy PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-input-key-policy PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-input-key-policy COMMAND windows-input-key-policy)
# This one existed for a while with nothing compiling it: no CMakeLists named
# the source, so it was absent from every build and every count, the same way
# msimeui-tests was. It passes as written.
add_executable(windows-tsf-key-dispatch tests/input/tsf_key_dispatch.cpp)
target_include_directories(windows-tsf-key-dispatch PRIVATE src src/system src/input)
if(MSVC)
  target_compile_options(windows-tsf-key-dispatch PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-tsf-key-dispatch PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-tsf-key-dispatch COMMAND windows-tsf-key-dispatch)
set_tests_properties(windows-tsf-key-dispatch PROPERTIES TIMEOUT 10)
add_executable(windows-candidate-completion-policy tests/input/candidate_completion_policy.cpp)
target_include_directories(windows-candidate-completion-policy PRIVATE src)
if(MSVC)
  target_compile_options(windows-candidate-completion-policy PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-candidate-completion-policy PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-candidate-completion-policy COMMAND windows-candidate-completion-policy)
add_executable(windows-candidate-text-policy tests/input/candidate_text_policy.cpp)
target_include_directories(windows-candidate-text-policy PRIVATE src)
if(MSVC)
  target_compile_options(windows-candidate-text-policy PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-candidate-text-policy PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-candidate-text-policy COMMAND windows-candidate-text-policy)
add_executable(windows-candidate-ui-action-policy tests/input/candidate_ui_action_policy.cpp)
target_include_directories(windows-candidate-ui-action-policy PRIVATE src)
if(MSVC)
  target_compile_options(windows-candidate-ui-action-policy PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-candidate-ui-action-policy PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-candidate-ui-action-policy COMMAND windows-candidate-ui-action-policy)
add_executable(windows-terminal-deactivation-policy tests/input/terminal_deactivation_policy.cpp)
target_include_directories(windows-terminal-deactivation-policy PRIVATE src)
if(MSVC)
  target_compile_options(windows-terminal-deactivation-policy PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-terminal-deactivation-policy PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-terminal-deactivation-policy COMMAND windows-terminal-deactivation-policy)
add_executable(windows-key-event-send-result tests/input/key_event_send_result.cpp)
target_include_directories(windows-key-event-send-result PRIVATE src)
if(MSVC)
  target_compile_options(windows-key-event-send-result PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-key-event-send-result PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-key-event-send-result COMMAND windows-key-event-send-result)
add_executable(windows-floating-toolbar-visibility tests/ui/floating_toolbar_visibility.cpp)
target_include_directories(windows-floating-toolbar-visibility PRIVATE src)
if(MSVC)
  target_compile_options(windows-floating-toolbar-visibility PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-floating-toolbar-visibility PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-floating-toolbar-visibility COMMAND windows-floating-toolbar-visibility)
add_executable(windows-fullscreen-foreground tests/core/fullscreen_foreground.cpp)
target_include_directories(windows-fullscreen-foreground PRIVATE src)
if(MSVC)
  target_compile_options(windows-fullscreen-foreground PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-fullscreen-foreground PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-fullscreen-foreground COMMAND windows-fullscreen-foreground)
set_tests_properties(windows-fullscreen-foreground PROPERTIES TIMEOUT 10)
add_executable(windows-floating-toolbar-placement tests/ui/floating_toolbar_placement.cpp)
target_include_directories(windows-floating-toolbar-placement PRIVATE src)
if(MSVC)
  target_compile_options(windows-floating-toolbar-placement PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-floating-toolbar-placement PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-floating-toolbar-placement COMMAND windows-floating-toolbar-placement)
add_executable(windows-toolbar-icons tests/ui/toolbar_icons.cpp)
target_include_directories(windows-toolbar-icons PRIVATE src)
if(MSVC)
  target_compile_options(windows-toolbar-icons PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-toolbar-icons PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-toolbar-icons COMMAND windows-toolbar-icons)
set_tests_properties(windows-toolbar-icons PROPERTIES TIMEOUT 10)
add_executable(windows-toolbar-mode-command tests/ui/toolbar_mode_command.cpp)
target_link_libraries(windows-toolbar-mode-command PRIVATE msime-windows-replies)
if(MSVC)
  target_compile_options(windows-toolbar-mode-command PRIVATE /W4 /WX /UNDEBUG)
else()
  target_compile_options(windows-toolbar-mode-command PRIVATE -Wall -Wextra -Werror -UNDEBUG)
endif()
add_test(NAME windows-toolbar-mode-command COMMAND windows-toolbar-mode-command)
set_tests_properties(windows-toolbar-mode-command PROPERTIES TIMEOUT 10)
add_executable(windows-toolbar-click tests/ui/toolbar_click.cpp)
if(MSVC)
  target_compile_options(windows-toolbar-click PRIVATE /W4 /WX /UNDEBUG)
else()
  target_compile_options(windows-toolbar-click PRIVATE -Wall -Wextra -Werror -UNDEBUG)
endif()
add_test(NAME windows-toolbar-click COMMAND windows-toolbar-click)
set_tests_properties(windows-toolbar-click PROPERTIES TIMEOUT 10)
add_executable(windows-toolbar-coordinates tests/ui/toolbar_coordinates.cpp)
if(MSVC)
  target_compile_options(windows-toolbar-coordinates PRIVATE /W4 /WX /UNDEBUG)
else()
  target_compile_options(windows-toolbar-coordinates PRIVATE -Wall -Wextra -Werror -UNDEBUG)
endif()
add_test(NAME windows-toolbar-coordinates COMMAND windows-toolbar-coordinates)
set_tests_properties(windows-toolbar-coordinates PROPERTIES TIMEOUT 10)
add_executable(windows-toolbar-layout tests/ui/toolbar_layout.cpp)
target_include_directories(windows-toolbar-layout PRIVATE src)
if(MSVC)
  target_compile_options(windows-toolbar-layout PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-toolbar-layout PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-toolbar-layout COMMAND windows-toolbar-layout)
set_tests_properties(windows-toolbar-layout PROPERTIES TIMEOUT 10)
add_executable(windows-shared-config-keybindings tests/runtime/shared_config_keybindings.cpp)
target_include_directories(windows-shared-config-keybindings PRIVATE src)
if(MSVC)
  target_compile_options(windows-shared-config-keybindings PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-shared-config-keybindings PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-shared-config-keybindings COMMAND windows-shared-config-keybindings)
set_tests_properties(windows-shared-config-keybindings PROPERTIES TIMEOUT 10)
add_executable(windows-candidate-menu-layout tests/ui/candidate_menu_layout.cpp)
target_include_directories(windows-candidate-menu-layout PRIVATE src)
if(MSVC)
  target_compile_options(windows-candidate-menu-layout PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-candidate-menu-layout PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-candidate-menu-layout COMMAND windows-candidate-menu-layout)
set_tests_properties(windows-candidate-menu-layout PROPERTIES TIMEOUT 10)
set_tests_properties(windows-floating-toolbar-placement PROPERTIES TIMEOUT 10)
add_executable(windows-candidate-card-size tests/ui/candidate_card_size.cpp)
target_include_directories(windows-candidate-card-size PRIVATE src)
if(MSVC)
  target_compile_options(windows-candidate-card-size PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-candidate-card-size PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-candidate-card-size COMMAND windows-candidate-card-size)
set_tests_properties(windows-candidate-card-size PROPERTIES TIMEOUT 10)
add_executable(windows-candidate-shadow tests/ui/candidate_shadow.cpp)
target_include_directories(windows-candidate-shadow PRIVATE src)
if(MSVC)
  target_compile_options(windows-candidate-shadow PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-candidate-shadow PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-candidate-shadow COMMAND windows-candidate-shadow)
set_tests_properties(windows-candidate-shadow PROPERTIES TIMEOUT 10)
add_executable(windows-candidate-wheel tests/ui/candidate_wheel.cpp)
target_include_directories(windows-candidate-wheel PRIVATE src)
if(MSVC)
  target_compile_options(windows-candidate-wheel PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-candidate-wheel PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-candidate-wheel COMMAND windows-candidate-wheel)
set_tests_properties(windows-candidate-wheel PROPERTIES TIMEOUT 10)
add_executable(windows-candidate-menu tests/ui/candidate_menu.cpp)
target_include_directories(windows-candidate-menu PRIVATE src)
if(MSVC)
  target_compile_options(windows-candidate-menu PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-candidate-menu PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-candidate-menu COMMAND windows-candidate-menu)
add_executable(windows-tray-menu-layout tests/ui/tray_menu_layout.cpp)
target_include_directories(windows-tray-menu-layout PRIVATE src)
if(MSVC)
  target_compile_options(windows-tray-menu-layout PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-tray-menu-layout PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-tray-menu-layout COMMAND windows-tray-menu-layout)
set_tests_properties(windows-tray-menu-layout PROPERTIES TIMEOUT 10)
add_executable(windows-tray-menu-dispatch tests/ui/tray_menu_dispatch.cpp)
target_include_directories(windows-tray-menu-dispatch PRIVATE src)
target_link_libraries(windows-tray-menu-dispatch PRIVATE Threads::Threads)
if(MSVC)
  target_compile_options(windows-tray-menu-dispatch PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-tray-menu-dispatch PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-tray-menu-dispatch COMMAND windows-tray-menu-dispatch)
add_executable(windows-aux-message tests/runtime/aux_message.cpp)
target_include_directories(windows-aux-message PRIVATE src)
if(MSVC)
  target_compile_options(windows-aux-message PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-aux-message PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-aux-message COMMAND windows-aux-message)
add_executable(windows-diagnostic-batch tests/runtime/diagnostic_batch.cpp)
target_include_directories(windows-diagnostic-batch PRIVATE src ../../vendor/MSIME-Engine/contracts)
if(MSVC)
  target_compile_options(windows-diagnostic-batch PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-diagnostic-batch PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-diagnostic-batch COMMAND windows-diagnostic-batch)
set_tests_properties(windows-diagnostic-batch PROPERTIES TIMEOUT 10)
add_executable(windows-mode-authority tests/input/mode_authority.cpp)
target_include_directories(windows-mode-authority PRIVATE src)
if(MSVC)
  target_compile_options(windows-mode-authority PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-mode-authority PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-mode-authority COMMAND windows-mode-authority)
set_tests_properties(windows-mode-authority PROPERTIES TIMEOUT 10)
# The presets and the slot fallbacks are shared/voice's, and so is the test now: it sat in this tree while
# both desktop hosts shipped the header, so nothing this host's gate runs ever checked it on the other one.
add_executable(windows-polish-prompt ../../shared/voice/tests/polish_prompt.cpp)
if(MSVC)
  target_compile_options(windows-polish-prompt PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-polish-prompt PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-polish-prompt COMMAND windows-polish-prompt)
set_tests_properties(windows-polish-prompt PROPERTIES TIMEOUT 10)
add_executable(windows-maintenance-hotkeys tests/runtime/maintenance_hotkeys.cpp)
target_include_directories(windows-maintenance-hotkeys PRIVATE src)
if(MSVC)
  target_compile_options(windows-maintenance-hotkeys PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-maintenance-hotkeys PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-maintenance-hotkeys COMMAND windows-maintenance-hotkeys)
set_tests_properties(windows-maintenance-hotkeys PROPERTIES TIMEOUT 10)
add_executable(windows-server-launch tests/runtime/server_launch.cpp)
target_include_directories(windows-server-launch PRIVATE src)
if(MSVC)
  target_compile_options(windows-server-launch PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-server-launch PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-server-launch COMMAND windows-server-launch)

add_executable(windows-installer-launch tests/core/installer_launch.cpp)
target_include_directories(windows-installer-launch PRIVATE src)
if(MSVC)
  target_compile_options(windows-installer-launch PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-installer-launch PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-installer-launch COMMAND windows-installer-launch
  "${CMAKE_CURRENT_SOURCE_DIR}/installer/msime_setup.iss")
add_executable(windows-shell-surfaces tests/ui/shell_surfaces.cpp)
target_include_directories(windows-shell-surfaces PRIVATE src)
if(MSVC)
  target_compile_options(windows-shell-surfaces PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-shell-surfaces PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-shell-surfaces COMMAND windows-shell-surfaces)
set_tests_properties(windows-shell-surfaces PROPERTIES TIMEOUT 10)
add_executable(windows-watchdog-policy tests/runtime/watchdog_policy.cpp)
target_include_directories(windows-watchdog-policy PRIVATE src)
if(MSVC)
  target_compile_options(windows-watchdog-policy PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-watchdog-policy PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-watchdog-policy COMMAND windows-watchdog-policy)
set_tests_properties(windows-watchdog-policy PROPERTIES TIMEOUT 10)
add_executable(windows-candidate-palette tests/ui/candidate_palette.cpp)
target_include_directories(windows-candidate-palette PRIVATE src)
if(MSVC)
  target_compile_options(windows-candidate-palette PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-candidate-palette PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-candidate-palette COMMAND windows-candidate-palette)
set_tests_properties(windows-candidate-palette PROPERTIES TIMEOUT 10)
add_library(msime-windows-replies STATIC src/ipc/ReplyCodec.cpp)
target_include_directories(msime-windows-replies PUBLIC src ../../vendor/MSIME-Engine/contracts)
if(MSVC)
  target_compile_options(msime-windows-replies PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(msime-windows-replies PRIVATE -Wall -Wextra -Werror)
endif()
option(MSIME_WINDOWS_PIPE_ONLY "Build native pipe tests without the Rust host library" OFF)
if(WIN32)
  add_subdirectory(tests/native-pipe)
endif()
if(MSIME_WINDOWS_PIPE_ONLY)
  if(NOT WIN32)
    message(FATAL_ERROR "Pipe-only tests require a Windows target")
  endif()
  return()
endif()
find_package(nlohmann_json 3.11 REQUIRED)
add_executable(windows-provider-token tests/runtime/provider_token.cpp)
target_include_directories(windows-provider-token PRIVATE src)
target_link_libraries(windows-provider-token PRIVATE nlohmann_json::nlohmann_json)
if(MSVC)
  target_compile_options(windows-provider-token PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-provider-token PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-provider-token COMMAND windows-provider-token)

add_executable(windows-first-run tests/runtime/first_run.cpp)
target_include_directories(windows-first-run PRIVATE src)
target_link_libraries(windows-first-run PRIVATE nlohmann_json::nlohmann_json)
if(MSVC)
  target_compile_options(windows-first-run PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-first-run PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-first-run COMMAND windows-first-run)
add_executable(windows-prepare-host tests/runtime/prepare_host.cpp)
target_include_directories(windows-prepare-host PRIVATE src)
target_link_libraries(windows-prepare-host PRIVATE nlohmann_json::nlohmann_json)
if(MSVC)
  target_compile_options(windows-prepare-host PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-prepare-host PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-prepare-host COMMAND windows-prepare-host)
find_package(CURL REQUIRED)
find_package(Threads REQUIRED)
add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/../../vendor/MSIME-Engine/voice
                 ${CMAKE_CURRENT_BINARY_DIR}/msime-engine-voice EXCLUDE_FROM_ALL)
# The voice subtree is excluded from the default build because we want its
# library, not its samples - but it registers its own ctest entries, and an
# excluded test executable is never built, so those entries reported "Not Run"
# on every single run. Either they build or they should not be registered;
# build them, because they cover the contracts this Server speaks.
foreach(voice_test test_voice test_voice_capture test_capture_devices test_doubao_protocol)
  if(TARGET ${voice_test})
    set_target_properties(${voice_test} PROPERTIES EXCLUDE_FROM_ALL FALSE)
  endif()
endforeach()
set(MSIME_HOST_LIBRARY "" CACHE FILEPATH "Built shared host library or Windows DLL import library")
if(NOT EXISTS "${MSIME_HOST_LIBRARY}")
  message(FATAL_ERROR "Set MSIME_HOST_LIBRARY to the Cargo-built shared host library for this target")
endif()
add_library(msime-windows-session STATIC src/ipc/ServerSession.cpp)
target_include_directories(msime-windows-session PUBLIC src ../../crates/host-api/include ../../vendor/MSIME-Engine/contracts)
target_link_libraries(msime-windows-session PUBLIC nlohmann_json::nlohmann_json Threads::Threads "${MSIME_HOST_LIBRARY}")
enable_testing()
add_executable(windows-session-smoke tests/runtime/session_smoke.cpp tests/runtime/session_pump.cpp tests/runtime/session_workers.cpp tests/core/preference_monitor.cpp tests/ui/candidate_mailbox.cpp tests/core/ui_selection.cpp)
add_executable(windows-reply-codec tests/core/reply_codec.cpp)
target_link_libraries(windows-reply-codec PRIVATE msime-windows-replies)
add_library(msime-windows-composer STATIC src/ipc/ReplyComposer.cpp src/input/ChineseTextConversion.cpp)
target_link_libraries(msime-windows-composer PUBLIC msime-windows-session msime-windows-replies)
add_library(msime-windows-focused STATIC src/input/FocusedSession.cpp)
target_link_libraries(msime-windows-focused PUBLIC msime-windows-composer)
add_library(msime-windows-input STATIC src/input/InputQueue.cpp)
target_link_libraries(msime-windows-input PUBLIC msime-windows-focused)
add_executable(windows-dedicated-english tests/input/dedicated_english.cpp)
target_link_libraries(windows-dedicated-english PRIVATE msime-windows-input)
if(MSVC)
  target_compile_options(windows-dedicated-english PRIVATE /W4 /WX /UNDEBUG)
else()
  target_compile_options(windows-dedicated-english PRIVATE -Wall -Wextra -Werror -UNDEBUG)
endif()
add_test(NAME windows-dedicated-english COMMAND windows-dedicated-english)
set_tests_properties(windows-dedicated-english PROPERTIES TIMEOUT 30)
add_library(msime-windows-pump STATIC src/ipc/SessionPump.cpp src/ipc/SessionWorkers.cpp src/ipc/SessionController.cpp src/system/PreferenceMonitor.cpp
            src/candidate/CloudCandidateWorker.cpp src/candidate/TranslationWorker.cpp
            src/candidate/AiCandidateWorker.cpp)
target_link_libraries(msime-windows-pump PUBLIC msime-windows-input CURL::libcurl)
add_executable(windows-dedicated-english-controller tests/input/dedicated_english_controller.cpp)
target_link_libraries(windows-dedicated-english-controller PRIVATE msime-windows-pump)
if(MSVC)
  target_compile_options(windows-dedicated-english-controller PRIVATE /W4 /WX /UNDEBUG)
else()
  target_compile_options(windows-dedicated-english-controller PRIVATE -Wall -Wextra -Werror -UNDEBUG)
endif()
add_test(NAME windows-dedicated-english-controller COMMAND windows-dedicated-english-controller)
set_tests_properties(windows-dedicated-english-controller PROPERTIES TIMEOUT 30)
# The cloud worker's queueing: it coalesces submissions inside its debounce and
# keeps only the newest, so the test spends real time waiting past that window.
# Which candidates the right-click actions may be offered for. All four write
# to the user dictionary, so offering one for a candidate the Engine will refuse
# leaves a menu item that does nothing.
add_executable(windows-candidate-action-availability tests/candidate/candidate_action_availability.cpp)
target_include_directories(windows-candidate-action-availability PRIVATE src/candidate)
if(MSVC)
  target_compile_options(windows-candidate-action-availability PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-candidate-action-availability PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-candidate-action-availability COMMAND windows-candidate-action-availability)
set_tests_properties(windows-candidate-action-availability PROPERTIES TIMEOUT 10)
add_executable(windows-cloud-candidate-worker tests/candidate/cloud_candidate_worker.cpp)
target_link_libraries(windows-cloud-candidate-worker PRIVATE msime-windows-pump)
target_include_directories(windows-cloud-candidate-worker PRIVATE src/candidate)
if(MSVC)
  target_compile_options(windows-cloud-candidate-worker PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-cloud-candidate-worker PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-cloud-candidate-worker COMMAND windows-cloud-candidate-worker)
set_tests_properties(windows-cloud-candidate-worker PROPERTIES TIMEOUT 60)
target_link_libraries(windows-session-smoke PRIVATE msime-windows-pump)
add_executable(windows-input-queue tests/input/input_queue.cpp)
target_link_libraries(windows-input-queue PRIVATE msime-windows-input)
add_test(NAME windows-input-queue COMMAND windows-input-queue)
set_tests_properties(windows-input-queue PROPERTIES TIMEOUT 30)
add_executable(windows-tsf-config-frames tests/runtime/tsf_config_frames.cpp)
target_link_libraries(windows-tsf-config-frames PRIVATE msime-windows-replies)
if(MSVC)
  target_compile_options(windows-tsf-config-frames PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-tsf-config-frames PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-tsf-config-frames COMMAND windows-tsf-config-frames)
set_tests_properties(windows-tsf-config-frames PROPERTIES TIMEOUT 10)
add_executable(windows-reply-composer tests/input/reply_composer.cpp)
add_executable(windows-voice-capture-selection tests/voice/voice_capture_selection.cpp)
target_link_libraries(windows-voice-capture-selection PRIVATE nlohmann_json::nlohmann_json)
if(MSVC)
  target_compile_options(windows-voice-capture-selection PRIVATE /UNDEBUG /W4 /WX)
else()
  target_compile_options(windows-voice-capture-selection PRIVATE -UNDEBUG -Wall -Wextra -Werror)
endif()
add_test(NAME windows-voice-capture-selection COMMAND windows-voice-capture-selection)
add_executable(windows-candidate-initialization tests/ui/candidate_initialization.cpp)
target_include_directories(windows-candidate-initialization PRIVATE src ../../vendor/MSIME-Engine/contracts)
target_link_libraries(windows-candidate-initialization PRIVATE msime-windows-session msime-windows-composer)
add_test(NAME windows-candidate-initialization COMMAND windows-candidate-initialization)
set_tests_properties(windows-candidate-initialization PROPERTIES TIMEOUT 10)
add_executable(windows-chinese-conversion tests/input/chinese_conversion.cpp)
target_include_directories(windows-chinese-conversion PRIVATE src ../../vendor/MSIME-Engine/contracts)
target_link_libraries(windows-chinese-conversion PRIVATE msime-windows-composer)
if(MSVC)
  target_compile_options(windows-chinese-conversion PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-chinese-conversion PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-chinese-conversion COMMAND windows-chinese-conversion)
set_tests_properties(windows-chinese-conversion PROPERTIES TIMEOUT 10)
add_executable(windows-preedit-caret tests/input/preedit_caret.cpp)
target_include_directories(windows-preedit-caret PRIVATE src ../../vendor/MSIME-Engine/contracts)
# src/candidate/CandidatePresentation.h reaches src/input/KeyEvent.h and so the host API headers; the
# session library is what carries those include paths.
target_link_libraries(windows-preedit-caret PRIVATE msime-windows-session msime-windows-composer)
if(MSVC)
  target_compile_options(windows-preedit-caret PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-preedit-caret PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-preedit-caret COMMAND windows-preedit-caret)
set_tests_properties(windows-preedit-caret PROPERTIES TIMEOUT 10)
add_executable(windows-candidate-skin tests/ui/candidate_skin.cpp)
target_include_directories(windows-candidate-skin PRIVATE src)
target_link_libraries(windows-candidate-skin PRIVATE nlohmann_json::nlohmann_json)
if(MSVC)
  target_compile_options(windows-candidate-skin PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-candidate-skin PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-candidate-skin COMMAND windows-candidate-skin)
set_tests_properties(windows-candidate-skin PROPERTIES TIMEOUT 10)
add_executable(windows-candidate-appearance tests/ui/candidate_appearance.cpp)
add_executable(windows-skin-resource-revision tests/ui/skin_resource_revision.cpp)
target_link_libraries(windows-skin-resource-revision PRIVATE nlohmann_json::nlohmann_json)
if(MSVC)
  target_compile_options(windows-skin-resource-revision PRIVATE /W4 /WX /UNDEBUG)
else()
  target_compile_options(windows-skin-resource-revision PRIVATE -Wall -Wextra -Werror -UNDEBUG)
endif()
add_test(NAME windows-skin-resource-revision COMMAND windows-skin-resource-revision)
set_tests_properties(windows-skin-resource-revision PROPERTIES TIMEOUT 10)
add_executable(windows-candidate-skin-reload tests/ui/candidate_skin_reload.cpp)
target_link_libraries(windows-candidate-skin-reload PRIVATE nlohmann_json::nlohmann_json Threads::Threads)
if(MSVC)
  target_compile_options(windows-candidate-skin-reload PRIVATE /W4 /WX /UNDEBUG)
else()
  target_compile_options(windows-candidate-skin-reload PRIVATE -Wall -Wextra -Werror -UNDEBUG)
endif()
add_test(NAME windows-candidate-skin-reload COMMAND windows-candidate-skin-reload)
set_tests_properties(windows-candidate-skin-reload PROPERTIES TIMEOUT 10)
add_executable(windows-candidate-theme-reload tests/ui/candidate_theme_reload.cpp)
target_link_libraries(windows-candidate-theme-reload PRIVATE nlohmann_json::nlohmann_json Threads::Threads)
if(MSVC)
  target_compile_options(windows-candidate-theme-reload PRIVATE /W4 /WX /UNDEBUG)
else()
  target_compile_options(windows-candidate-theme-reload PRIVATE -Wall -Wextra -Werror -UNDEBUG)
endif()
add_test(NAME windows-candidate-theme-reload COMMAND windows-candidate-theme-reload)
set_tests_properties(windows-candidate-theme-reload PROPERTIES TIMEOUT 10)
add_executable(windows-floating-toolbar-reload tests/ui/floating_toolbar_reload.cpp)
target_link_libraries(windows-floating-toolbar-reload PRIVATE nlohmann_json::nlohmann_json Threads::Threads)
if(MSVC)
  target_compile_options(windows-floating-toolbar-reload PRIVATE /W4 /WX /UNDEBUG)
else()
  target_compile_options(windows-floating-toolbar-reload PRIVATE -Wall -Wextra -Werror -UNDEBUG)
endif()
add_test(NAME windows-floating-toolbar-reload COMMAND windows-floating-toolbar-reload)
set_tests_properties(windows-floating-toolbar-reload PROPERTIES TIMEOUT 10)
add_executable(windows-candidate-layout-reload tests/ui/candidate_layout_reload.cpp)
target_link_libraries(windows-candidate-layout-reload PRIVATE nlohmann_json::nlohmann_json)
if(MSVC)
  target_compile_options(windows-candidate-layout-reload PRIVATE /W4 /WX /UNDEBUG)
else()
  target_compile_options(windows-candidate-layout-reload PRIVATE -Wall -Wextra -Werror -UNDEBUG)
endif()
add_test(NAME windows-candidate-layout-reload COMMAND windows-candidate-layout-reload)
set_tests_properties(windows-candidate-layout-reload PROPERTIES TIMEOUT 10)
add_executable(windows-candidate-font-reload tests/ui/candidate_font_reload.cpp)
target_link_libraries(windows-candidate-font-reload PRIVATE nlohmann_json::nlohmann_json Threads::Threads)
if(MSVC)
  target_compile_options(windows-candidate-font-reload PRIVATE /W4 /WX /UNDEBUG)
else()
  target_compile_options(windows-candidate-font-reload PRIVATE -Wall -Wextra -Werror -UNDEBUG)
endif()
add_test(NAME windows-candidate-font-reload COMMAND windows-candidate-font-reload)
set_tests_properties(windows-candidate-font-reload PROPERTIES TIMEOUT 10)
target_include_directories(windows-candidate-appearance PRIVATE src)
# src/system/PreviewConfig.h reaches src/input/KeyEvent.h and so the host API headers; the session
# library is what carries those include paths, as windows-preview-config does.
target_link_libraries(windows-candidate-appearance PRIVATE msime-windows-session)
if(MSVC)
  target_compile_options(windows-candidate-appearance PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-candidate-appearance PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-candidate-appearance COMMAND windows-candidate-appearance)
set_tests_properties(windows-candidate-appearance PROPERTIES TIMEOUT 10)
add_executable(windows-preview-config tests/ui/preview_config.cpp)
target_link_libraries(windows-preview-config PRIVATE msime-windows-session)
add_test(NAME windows-preview-config COMMAND windows-preview-config)
target_link_libraries(windows-reply-composer PRIVATE msime-windows-composer)
foreach(target msime-windows-session windows-session-smoke windows-reply-codec msime-windows-composer windows-reply-composer msime-windows-focused msime-windows-input windows-input-queue msime-windows-pump windows-preview-config)
  if(MSVC)
    target_compile_options(${target} PRIVATE /W4 /WX /utf-8)
  else()
    target_compile_options(${target} PRIVATE -Wall -Wextra -Werror)
  endif()
endforeach()
# The clipboard store and its presentation mailbox. All three of these sat in
# the tree with no CMakeLists naming them, so nothing compiled them and they
# were absent from every build and every count - the same shape as the
# msimeui-tests entry in known-failures.txt. Registered outside the WIN32-only
# Server block below, because none of them needs the Server. Written out one by
# one rather than generated: the name of a test source should be findable by
# searching for it.
add_library(msime-windows-clipboard STATIC src/clipboard/ClipboardHistory.cpp
            src/clipboard/ClipboardPresentation.cpp)
target_include_directories(msime-windows-clipboard PUBLIC src src/clipboard)
target_link_libraries(msime-windows-clipboard PUBLIC nlohmann_json::nlohmann_json)
add_executable(windows-clipboard-history tests/clipboard/clipboard_history.cpp)
target_link_libraries(windows-clipboard-history PRIVATE msime-windows-clipboard)
add_executable(windows-clipboard-link tests/clipboard/clipboard_link.cpp)
target_link_libraries(windows-clipboard-link PRIVATE msime-windows-clipboard)
add_executable(windows-clipboard-presentation tests/clipboard/clipboard_presentation.cpp)
target_link_libraries(windows-clipboard-presentation PRIVATE msime-windows-clipboard)
foreach(clipboard_target windows-clipboard-history windows-clipboard-link
        windows-clipboard-presentation)
  if(MSVC)
    target_compile_options(${clipboard_target} PRIVATE /W4 /WX /utf-8)
  else()
    target_compile_options(${clipboard_target} PRIVATE -Wall -Wextra -Werror)
  endif()
  add_test(NAME ${clipboard_target} COMMAND ${clipboard_target})
  set_tests_properties(${clipboard_target} PROPERTIES TIMEOUT 30)
endforeach()
if(WIN32)
  install(FILES installer/config.default.toml DESTINATION share/msime)
  install(DIRECTORY installer/assets/icons/ DESTINATION share/msime/icons)
  install(DIRECTORY installer/assets/audios/ DESTINATION share/msime/audios)
  add_subdirectory(msimeui)
  add_subdirectory(tsf)
  find_package(ZLIB REQUIRED)
  find_package(SQLite3 REQUIRED)
  if(TARGET SQLite3::SQLite3)
    set(MSIME_SQLITE_TARGET SQLite3::SQLite3)
  else()
    set(MSIME_SQLITE_TARGET SQLite::SQLite3)
  endif()
  add_library(msime-windows-server STATIC src/system/WindowsServer.cpp src/candidate/CandidateWindow.cpp src/candidate/CandidateFlyoutWindow.cpp src/candidate/TrayMenuWindow.cpp)
  # src/system/server_main.cpp constructs ClipboardHistory and ClipboardMonitor, whose definitions
  # are out of line. Without these the Server target does not link at all.
  target_sources(msime-windows-server PRIVATE src/clipboard/ClipboardHistory.cpp src/clipboard/ClipboardMonitor.cpp)
  target_sources(msime-windows-server PRIVATE src/candidate/FloatingToolbarWindow.cpp src/system/ShellLauncher.cpp
                 src/system/WaveOverlay.cpp src/system/WaveOverlayUtils.cpp src/voice/VoiceInputSession.cpp
                 src/voice/DoubaoAsrClient.cpp src/voice/CuePlayer.cpp src/voice/SystemAudioMuter.cpp
                 src/voice/VoiceHotkey.cpp src/input/MaintenanceHotkey.cpp src/voice/VoiceControlMessage.cpp)
  add_subdirectory(../../shared/voice shared-voice)
  target_include_directories(msime-windows-server PRIVATE
                             ../../vendor/MSIME-Engine/voice/third_party/miniaudio)
  target_link_libraries(msime-windows-server PUBLIC msime-windows-pump msime-windows-pipe
                         msimeui user32 gdi32 d2d1 dwrite dwmapi shell32
                         MetasequoiaIme::VoiceCapture MetasequoiaIme::Voice msime-voice-providers
                         CURL::libcurl winhttp ole32 uuid ZLIB::ZLIB)
  add_executable(windows-clipboard-text tests/clipboard/clipboard_text.cpp)
  target_include_directories(windows-clipboard-text PRIVATE src)
  target_link_libraries(windows-clipboard-text PRIVATE msime-windows-server)
  if(MSVC)
    target_compile_options(windows-clipboard-text PRIVATE /W4 /WX /utf-8)
  else()
    target_compile_options(windows-clipboard-text PRIVATE -Wall -Wextra -Werror)
  endif()
  add_test(NAME windows-clipboard-text COMMAND windows-clipboard-text)
  set_tests_properties(windows-clipboard-text PROPERTIES TIMEOUT 10)
  add_executable(windows-voice-providers tests/voice/voice_providers.cpp)
  target_include_directories(windows-voice-providers PRIVATE src)
  target_link_libraries(windows-voice-providers PRIVATE msime-windows-server)
  add_test(NAME windows-voice-providers COMMAND windows-voice-providers)
  set_tests_properties(windows-voice-providers PROPERTIES TIMEOUT 10)
  add_executable(windows-server-smoke tests/runtime/server_smoke.cpp)
  add_executable(msime-client-watchdog WIN32 src/system/Watchdog.cpp)
  # Match the installed logon task and uninstall process-stop contract.
  set_target_properties(msime-client-watchdog PROPERTIES OUTPUT_NAME MetasequoiaImeWatchdog)
  target_include_directories(msime-client-watchdog PRIVATE src)
  target_link_libraries(msime-client-watchdog PRIVATE shell32 ole32 uuid)
  if(MSVC)
    target_compile_options(msime-client-watchdog PRIVATE /W4 /WX /utf-8)
  else()
    target_compile_options(msime-client-watchdog PRIVATE -Wall -Wextra -Werror)
    target_link_options(msime-client-watchdog PRIVATE -municode -mwindows)
  endif()
  if(MSVC)
    set_target_properties(msime-client-watchdog PROPERTIES WIN32_EXECUTABLE ON)
  endif()
  add_test(NAME windows-watchdog-help COMMAND msime-client-watchdog --help)
  set_tests_properties(windows-watchdog-help PROPERTIES TIMEOUT 5)
  add_executable(msime-client-server src/system/server_main.cpp)
  # The product mark, which the toolbar loads out of the running executable.
  # Compiled into the Server itself rather than shipped beside it: an icon the
  # installer could forget to copy is an icon that silently stops appearing.
  enable_language(RC)
  target_sources(msime-client-server PRIVATE ServerResources.rc)
  target_include_directories(msime-client-server PRIVATE "${CMAKE_CURRENT_LIST_DIR}")
  set_property(SOURCE ServerResources.rc APPEND PROPERTY OBJECT_DEPENDS
    "${CMAKE_CURRENT_LIST_DIR}/src/ipc/ServerResources.h"
    "${CMAKE_CURRENT_LIST_DIR}/installer/assets/icons/msime.ico")
  include(ServerManifest.cmake)
  add_executable(msime-client-prepare src/system/prepare_host_main.cpp)
  target_include_directories(msime-client-prepare PRIVATE ../../crates/host-api/include)
  target_link_libraries(msime-client-prepare PRIVATE
    nlohmann_json::nlohmann_json "${MSIME_HOST_LIBRARY}")
  if(MSVC)
    target_compile_options(msime-client-prepare PRIVATE /W4 /WX /utf-8)
  else()
    target_compile_options(msime-client-prepare PRIVATE -Wall -Wextra -Werror)
  endif()
  if(MINGW)
    target_link_options(msime-client-prepare PRIVATE -municode)
  endif()
  add_test(NAME windows-prepare-help COMMAND msime-client-prepare --help)
  set_target_properties(msime-client-server PROPERTIES OUTPUT_NAME MetasequoiaImeServer)
  target_link_libraries(msime-client-server PRIVATE msime-windows-server)
  add_test(NAME windows-preview-help COMMAND msime-client-server --help)
  set_tests_properties(windows-preview-help PROPERTIES TIMEOUT 5)
  if(MINGW)
    target_link_options(msime-client-server PRIVATE -municode)
    target_link_options(windows-session-smoke PRIVATE -municode)
  endif()
  target_link_libraries(windows-server-smoke PRIVATE msime-windows-server)
  foreach(target msime-windows-server windows-server-smoke msime-client-server)
    # Keep TCHAR/D2D declarations wide in every translation unit. Link-only
    # -municode does not set these macros when compiling the Server library.
    target_compile_definitions(${target} PRIVATE UNICODE _UNICODE NOMINMAX
                               WIN32_LEAN_AND_MEAN)
    if(MSVC)
      # Vendored Engine and system headers are not ours to warn-clean; keep
      # /W4 /WX for this repository's own sources only.
      target_compile_options(${target} PRIVATE /W4 /WX /utf-8
                             /external:anglebrackets /external:W0)
    else()
      target_compile_options(${target} PRIVATE -Wall -Wextra -Werror)
    endif()
  endforeach()
  add_test(NAME windows-server COMMAND windows-server-smoke)
  set_tests_properties(windows-server PROPERTIES TIMEOUT 30)
  install(TARGETS msime-client-server msime-client-watchdog msime-client-prepare
          RUNTIME DESTINATION bin)
endif()
add_test(NAME windows-session COMMAND windows-session-smoke)
add_executable(windows-runner-probe tests/runtime/runner_probe.cpp)
target_compile_features(windows-runner-probe PRIVATE cxx_std_17)
if(NOT CMAKE_CROSSCOMPILING)
  find_program(MSIME_POWERSHELL NAMES pwsh powershell)
  if(MSIME_POWERSHELL)
    add_test(NAME windows-runner-control
      COMMAND "${MSIME_POWERSHELL}" -NoProfile -File
        "${CMAKE_CURRENT_SOURCE_DIR}/tests/tools/runner_regression.ps1"
        -Probe $<TARGET_FILE:windows-runner-probe>)
    set_tests_properties(windows-runner-control PROPERTIES TIMEOUT 30)
  endif()
endif()
set_tests_properties(windows-session PROPERTIES TIMEOUT 30)
add_test(NAME windows-reply-codec COMMAND windows-reply-codec)
add_test(NAME windows-reply-composer COMMAND windows-reply-composer)

add_executable(windows-voice-control-message
               src/voice/VoiceControlMessage.cpp tests/voice/voice_control_message.cpp)
target_include_directories(windows-voice-control-message PRIVATE src ../../vendor/MSIME-Engine/contracts)
if(MSVC)
  target_compile_options(windows-voice-control-message PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-voice-control-message PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-voice-control-message COMMAND windows-voice-control-message)

# The keyboard hook's decisions, with no Win32 in them. The hook itself cannot
# run off Windows, and nothing covered these rules at all - a modifier leaking
# to the application after being used as a shortcut, or a hold that keeps
# recording after the user let go, are one-branch mistakes.
add_executable(windows-voice-hotkey-policy tests/voice/voice_hotkey_policy.cpp)
target_include_directories(windows-voice-hotkey-policy PRIVATE src/voice)
if(MSVC)
  target_compile_options(windows-voice-hotkey-policy PRIVATE /W4 /WX /utf-8)
else()
  target_compile_options(windows-voice-hotkey-policy PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME windows-voice-hotkey-policy COMMAND windows-voice-hotkey-policy)
set_tests_properties(windows-voice-hotkey-policy PROPERTIES TIMEOUT 10)
add_executable(windows-voice-session-epoch tests/voice/voice_session_epoch.cpp)
target_link_libraries(windows-voice-session-epoch PRIVATE Threads::Threads)
add_test(NAME windows-voice-session-epoch COMMAND windows-voice-session-epoch)
set_tests_properties(windows-voice-session-epoch PROPERTIES TIMEOUT 10)

# Tests in this tree check with assert(), but the suite is configured, built and
# run in Release (platforms/windows/README.md), where NDEBUG compiles every one
# of them away. That cost more than coverage: three targets stopped building
# because /WX saw the variables an elided assert had left unused, and the tests
# that still built were reporting success without checking anything. Keep the
# assertions for the test executables only - the Server, the TIP and msimeui
# keep NDEBUG, so nothing shipped gains an assert.
get_property(msime_windows_tests DIRECTORY PROPERTY TESTS)
foreach(msime_windows_test IN LISTS msime_windows_tests)
  if(TARGET ${msime_windows_test})
    target_compile_options(${msime_windows_test} PRIVATE
                           $<IF:$<CXX_COMPILER_ID:MSVC>,/UNDEBUG,-UNDEBUG>)
  endif()
endforeach()
